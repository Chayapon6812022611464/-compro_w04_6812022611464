#include <stdio.h>  // ไม่มี # และ < > ใช้เรียก Library

int main() {                         
    int a = 10; // ไม่มี ; 
    printf("a = %d\n", a); // ไม่มี ; เเละ \n
    return 0; // ไม่มี ; 
}